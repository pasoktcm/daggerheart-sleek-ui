<div class="resource quantity-resource" data-item-uuid="{{item.uuid}}">
  <input
    type="number"
    class="inventory-item-quantity"
    value="{{item.system.quantity}}"
    min="0"
    step="1"
  />
</div>
